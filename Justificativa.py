from datetime import datetime


class Justificativa:
    def __init__(self, matricula, nome_destinatario,):
        self.matricula = matricula
        self.nome_destina
        data_formatada = datetime.now().strftime('%d/%m/%Y %H:%M')
        self.data = data_formatada